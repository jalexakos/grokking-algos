// 5.1-5.4: Which of these hash functions are consistent?

// 5.1

// f(x) = 1 
// consistent

// 5.2

// f(x) = Math.random()
// inconsistent

// 5.3

// f(x) = nextEmptySlot()
// inconsistent

// 5.4

// f(x) = len(x)
// consistent

// 5.5

// in this case, use the letter and prime number mapping for your hash function (D).

// 5.6

// in this case, the hash function that uses the length of the string as the index would be the best (B).

// 5.7

// in this case, use the first character of the string as the index for your hash function (C).